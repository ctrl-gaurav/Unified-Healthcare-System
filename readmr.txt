pip install flask
To activate virtual enviournment : source venv/bin/activate

index.html: for loop and if else using jinja2
<ul>
    {% for fruits in fruits %}
    <li> {{fruits}} </li>
    {% endfor %}

    {% if 'mango' in fruits %}
        <p> Mango exists</p>
    {% endif %}
</ul>

pip3 install flask-bootstrap


Load iamge: <img src= {{url_for('static', filename='logo.png')}} height="50" width="100" />

# config sqllite
app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite:///healthBeats.db'
app.config['SQLALCHEMY_Track_Modification'] = False
db = SQLAlchemy(app)

class HealthBeats(db.Model):
    sno = db.Column(db.Integer, primary_key=True)
    title = db.Column(db.String(200), nullable=False)
    desc = db.Column(db.String(500), nullable=False)
    date_created = db.Column(db.DateTime, default=datetime.utcnow)

    def __repr__(self) -> str:
        return f"{self.sno} - {self.title}"